esp32 code


#include "esp_camera.h"

// ===================
// FLASH LED PIN
// ===================
#define FLASH_LED_PIN 4   // GPIO4 controls flash LED

// ===================
// CAMERA CONFIG (AI THINKER)
// ===================
camera_config_t config = {
  .pin_pwdn = 32,
  .pin_reset = -1,
  .pin_xclk = 0,
  .pin_sscb_sda = 26,
  .pin_sscb_scl = 27,

  .pin_d7 = 35,
  .pin_d6 = 34,
  .pin_d5 = 39,
  .pin_d4 = 36,
  .pin_d3 = 21,
  .pin_d2 = 19,
  .pin_d1 = 18,
  .pin_d0 = 5,
  .pin_vsync = 25,
  .pin_href = 23,
  .pin_pclk = 22,

  .xclk_freq_hz = 20000000,
  .ledc_timer = LEDC_TIMER_0,
  .ledc_channel = LEDC_CHANNEL_0,
  .pixel_format = PIXFORMAT_JPEG,
  .frame_size = FRAMESIZE_QVGA,
  .jpeg_quality = 12,
  .fb_count = 1
};

void setup() {
  Serial.begin(115200);
  delay(500);

  // ===================
  // FORCE FLASH OFF
  // ===================
  pinMode(FLASH_LED_PIN, OUTPUT);
  digitalWrite(FLASH_LED_PIN, LOW);   // FLASH OFF

  // ===================
  // CAMERA INIT
  // ===================
  esp_err_t err = esp_camera_init(&config);
  if (err != ESP_OK) {
    Serial.println("Camera init failed");
    while (true);
  }

  Serial.println("ESP32-CAM READY");
  Serial.println("FLASH OFF");
}

void loop() {
  // ===================
  // PLACEHOLDER FLOWER DETECTION
  // (Replace with AI / color detection later)
  // ===================
  bool flowerDetected = true;  // simulated detection

  if (flowerDetected) {
    Serial.println("P");   // Command to Arduino → Pollination
    delay(5000);           // Prevent repeated triggers
  }
}
